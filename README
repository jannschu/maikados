How to build (client)
=====================

You need CoffeScript (tested with 1.1.1).
Run `cake build' after cloning the repo. 

Now open the `index.html' to see the client.

Hints for developers (client)
=============================

- use `cake autobuild' ;-)
- use the coding style already there please